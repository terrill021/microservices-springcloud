# Properties active for ALL profiles:
---
spring:
  application:
    name: sentence
  cloud:
    config:
      uri: http://localhost:1337	# Expect the Spring Cloud Config server to be running on localhost, port 8001.  
server:
  port: 8020      # Fix the sentence server to 8020.  This makes it easy to find.